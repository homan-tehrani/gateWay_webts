version: '3.8'

services:
  getway:
    container_name: getway
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - 8085:8000
    networks:
      - network_api

networks:
  network_api:
    driver: overley
    external: true

